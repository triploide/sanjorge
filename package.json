{
  "name": "san-jorge-suspension",
  "version": "1.0.0",
  "description": "",
  "main": "api/main.js",
  "scripts": {
    "start": "electron ./api/main.js",
    "build-windows": "electron-packager ./src --platform=win32 --out=./dist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maximiliano Yañez",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.6.1"
  }
}
